## 1. 精确规模配置与拓扑

- [x] 1.1 在配置与工作流中接入显式 AS-Beneš 模式及独立示例/输出路径；验证精确模式拒绝内外端口不等和非法 N，标准模式配置、哈希及原示例继续有效。
- [x] 1.2 实现对称 floor/ceil 递归、奇数两端旁路和稳定端口/器件标识；以 N=1、2、3、5、13、25、100、128 检查递归计数与连接完整性，确认 N=100 为 596 个 MZI 和最多 13 列。
- [x] 1.3 实现带未配对端点约束的置换求解及部分请求补全，记录可重排性的递归依据；验证重复输入/输出、越界及非法状态拒绝，合法部分请求保留正确暗输入。
- [x] 1.4 增加独立端口走图验证及真实路径级数导出；通过 N=2…8 全置换、N=100 全部 10000 个单连接、恒等/反序/100 个循环移位/种子 17 的 100 个随机全置换，并确认损坏旁路或状态被拒绝。

## 2. 光学层级与直接置换

- [x] 2.1 将 AS 端口图接入物理列及旁路位置分配，允许每列器件数不同和浅子网跨列直通；检查 5、7、12、13 路映射及 100 路端口表无补齐通道或悬空端点。
- [x] 2.2 参数化 34/35 µm MZI 光学端口间距，复用 1 mm 光学本体及原 crossing 单元；局部几何检查确认有源长 680 µm、总长 1000 µm、R≥20 µm 和 20×20 µm crossing footprint。
- [x] 2.3 生成偶数及奇数 floor/ceil 连续斜线置换模板，消除规则入口的额外补偿 S 弯；测试正向/逆向端口映射、量化后切线/半径/净距及旁路接续，输出奇数必要额外转弯记录。

## 3. 左右电学变体与共享 G

- [x] 3.1 拆分光学本体、独立 S 引出和列级 G 所有权，生成左/右电学变体及独立器件预览；验证两变体光学端口/状态含义一致、S 直线出线及局部金属隔离。
- [x] 3.2 根据实际光学障碍形成共享 G 连续段并合并有源区金属，每轨仅放一个落点与一组 via；检查 m 个连续 MZI 有 m+1 个 G 接点、两器件由四接点减为三接点，旁路处分段且每个 G 电极探针仍连通。
- [x] 3.3 实现按列方向的 G 汇流与共同级间 S 轨道预留，允许符合规则的 M2 跨 crossing 区布线；用相向引出列验证轨道容量、S-G/S-S 隔离及 via 禁布区，确认不靠额外局部折返和重复换层接通。

## 4. 两排错位 pad 与尺寸搜索

- [x] 4.1 实现每侧两排 100 µm 格点、50 µm 横向错位、至少 70 µm 行距及分布式 G 占位；100 路验证南北各 298 S+4 G、每排 151 个、总计 604 个 pad，小规模配置验证数量和空间分配不依赖固定 100 路。
- [x] 4.2 将 pad 顺序分配、格点整体偏移和列左右引出联合估算，按设计规定每种间距最多 8 个候选加全右出对照进入完整生成；验证候选数量上限 18、结果确定性及真实几何评分符合面积/宽度/转折/线长/路径差异顺序。
- [x] 4.3 扩展指标与候选对比报告，分别计算光学/金属/pad/整片包络及路径分布；检查 15.110 mm 只标注 pad 区估算，20 mm 作为软目标，失效或增大的候选如实记录。

## 5. 全片验证与交付

- [x] 5.1 扩展独立 GDS 读回以识别 AS 旁路、成对贯通 crossing 和共享 G 探针；用 5、7、12、13 路版图检查连通及几何，并用共享 G 开路、S-G 桥接、漏/重复接点、旁路错接和非法弯曲故障确认验证器能拒绝。
- [x] 5.2 完成 100 路候选生成、选择和全片验收，确认 100 输入/输出、596 个 MZI、604 个 pad、597 个电学网络、R≥20 µm及全部路径连接；报告最终 34/35 µm 选择、逐列引出侧、G 接点数和实际尺寸。
- [x] 5.3 重复生成选定配置两次，比较规范化后的几何、连接、状态及指标；运行现有回归检查，确认标准 128 路参考契约及输出未被新模式破坏。
- [x] 5.4 交付新示例、GDS、端口/连接/控制数据、总览与左右引线/共享 G/奇数旁路/pad 局部图，更新使用说明和相对 22.462870×4.484092 mm 基线的对比报告；检查产物链接、参数来源及未执行的性能验证说明准确。

## 6. Pad 直连与布线规整（用户确认的增量优化）

- [x] 6.1 两排错位 pad 改为末端 M2 直接接入，删除统一的 M2–M1–M2 换层；检查内外排净距、全部 S/G 连通和 via 数变化。
- [x] 6.2 根据用户追加要求按引线调整两排 pad 位置，保持同排至少 100 µm，在原选定光学布局及布线高度预算内规整通道顺序，减少横纵跨层穿插及列内阶梯的反向跳变；保留硬性避让约束，报告量化前后结果并提供左右端局部图。
- [x] 6.3 生成完整 100 路增量版本并独立读回，运行相关回归及可复现检查，更新设计说明与交付文档。结果为 20.252711×4.555112 mm、2528 个 via、pad 转接区横纵投影交叉为 0；428 项回归通过，重复生成一致。
