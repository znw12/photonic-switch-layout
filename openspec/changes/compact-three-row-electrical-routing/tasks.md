## 1. 实现与小规模验证
- [x] 1.1 新增 three-row 配置和对齐偏置，复用居中分层路由并保持 two-row 兼容，通过参数及小规模物理测试。
- [x] 1.2 扩展三排末列、错位、via 和跨越窗口验证，通过故障注入及完整回归。

## 2. 优化与交付
- [x] 2.1 有限扫描 stage 对齐偏置，生成共享基线、选定改进和独立引出完整候选，保存实际尺寸比较。
- [x] 2.2 最佳候选重复生成并独立回读，核对几何、设置及端口/pad 表一致；旧两排 bundle 回读通过。
- [x] 2.3 保存示例、报告、GDS 和预览，更新说明，验证 OpenSpec 并提交 Git。
