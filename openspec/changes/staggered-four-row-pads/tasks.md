## 1. 参数与 pad 放置

- [x] 1.1 增加四排单条带支持、缺省零值的 `pad_row_stagger` 配置和命令行选项；用测试确认 25 μm/零错位有效，负值、非有限值、非网格值、跨列偏移及不支持的模式组合被明确拒绝，原有默认配置仍可加载。
- [x] 1.2 建立确定性的四排 pad 槽位表，固定南北内排向外编号和全局 +x 错位方向；检查默认每侧四排各 208 个、相邻排 25 μm 错位及末列不满/不足四个 pad 时无重复或虚构网络。

## 2. 路由与边界

- [x] 2.1 为实际错位 pad 分配独立 M1 引线、M2 干线和每 net 三个 via；以小规模 25 μm 错位和零错位四排 GDS 提取证明端子一一连通、无开短路，间距及 enclosure 合格。
- [x] 2.2 用实际 pad 槽位包围盒更新整片边界和外部光接口位置；验证 100 端口默认 bank 为 20.835 × 0.360 mm，最外排 pad、扇出、终止器均包含在 GDS 全局边界内。
- [x] 2.3 扩展 pad CSV/manifest、报告与局部预览；检查导出记录包含行列号、排偏移、实际中心和 via 坐标，图中可辨认四排错位，报告区分 pad-only 跨度与整片尺寸。

## 3. 独立验证与完整规模实验

- [x] 3.1 扩展独立验证并加入错位错误、漏排/重复 pad、缺失 via、异网 pad 下误加 via、过小间距和遗漏外排边界等反例；确认全部缺陷被拒绝，不能仅靠 manifest 自洽通过。
- [x] 3.2 生成单条带 100 活动/128 内部端口的四排 25 μm 错位版和零错位对照；两者均通过规范图、光学几何、GDS 层级/多边形回读及 1664 nets/4992 vias 电气提取，并保存配置、GDS、预览和报告。
- [x] 3.3 与既有两排/三排单条带结果比较实际宽高、面积、pad 跨度、扇出范围和路径统计；核对 MZI 放置及级间光学几何保持一致，单独记录外部 IO 延伸导致的长度变化。
- [x] 3.4 再次独立生成完整四排错位版，核对规范化几何 hash、pad 映射、尺寸和路径统计一致；运行完整回归，保存新增测试和旧模式兼容性结果。

## 4. 交付

- [x] 4.1 更新使用文档、可复现示例和测量报告，明确 25 μm 错位方向、20.835 mm 的含义与占位工艺假设；严格校验 OpenSpec，复核任务验收证据后提交实现及报告到 Git。
