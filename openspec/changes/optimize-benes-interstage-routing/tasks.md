## 1. 参数、兼容性与产物契约

- [x] 1.1 在现有配置与序列化入口增加 `legacy | continuous | compressed` 模式和独立 `shuffle_pitch`；实现模式适用范围、有限值、网格和参数组合校验，以旧配置/旧 bundle 回读及非法参数拒绝作为验收。
- [x] 1.2 扩展路由 manifest，记录基本段、方向、端口切向、crossing 对穿关系和各边界策略；补充单元缓存键及允许方向契约，验证不同模式/间距不会串用 cell，重复模块仍复用层级。

## 2. A：连续斜线与独立验证

- [x] 2.1 在 `geometry.Library.shuffle_block` 对应路径实现连续斜线 SPLIT/MERGE，合并重复弯曲并保留独立 CROSSING 实例；对 4/8/16/32/64/128 通道正反模块逐端口核对规范置换，记录实际宽度和基本段数量。
- [x] 2.2 扩展 `verify` 的方向变换、基本段检查和中心线重建，独立重算长度、弯曲、半径与 crossing 数；验证 45° 端口网格、切向连续性以及半径下限 20 μm，拒绝器件未声明允许的朝向。
- [x] 2.3 将 crossing 豁免限制为窗口内所属两条对穿轨迹，保留邻近对象间距检查；以错误对穿、断点、切向折角、过小半径、未声明相交、第三条波导侵入及统计篡改的故障注入确认检查有效。
- [x] 2.4 将 A 接入单条带 `reshape` 流程，按实际模块宽度重新计算级位置、电学逃逸与四排错位 pad；生成小规模完整 GDS，通过光路映射和真实金属回读后再进入全尺寸比较。

## 3. B：局部压缩与真实过渡成本

- [x] 3.1 实现保持波导顺序的 fan-in/fan-out 及小位移解析 S 弯，分离器件行距和局部光学间距；验证零位移、小于两倍半径的位移、最大束收缩的端点切向、全程间距及曲率，不直接复用受限的折叠连接。
- [x] 3.2 组合收束、连续交叉区和展开，逐规模比较完整模块宽度，记录压缩或 A 回退的边界选择；验证 q=60 零收束对照和至少一个 q<60 模块，确认报告包含两端过渡开销且没有隐藏额外交叉。
- [x] 3.3 增加 q=40/45/50/55/60 μm 的独立参考配置与完整布局入口，保持相同 gap/corridor/pad 对照条件；确保每个 q 实际执行且不被现有 `max_candidates=1` 截断，失败与全 A 回退均有明确标记。

## 4. 完整候选比较与可复现产物

- [x] 4.1 在独立的 `examples/benes/interstage/` 和 `output/benes/interstage/` 组织候选，不覆盖 distributed 基线；输出完整 die 宽高/面积、逐边界核心及过渡宽度、实际间距、全局/每路径 crossing、圆弧与路径长度、pad/fanout 外框、耗时、GDS 大小和规范化 hash。
- [x] 4.2 扩展比较报告和预览，按现有完整 die 整数网格面积优先、均匀性其次排序，仅推荐完整验证通过的候选；用基线胜出、失败候选及宽度/面积取舍检查排序与展示，明确区分解析估算和实际测量。
- [x] 4.3 运行旧基线、A 和全部 B 参考配置的 100/128 完整比较；A 和至少一个实际使用 q<60 的 B 候选完成独立物理验证，核对 832 MZI、1664 pad、4992 via、56 备用终止、13 级与 7680 crossing。若 B 无合法压缩候选，记录阻塞原因并保持本任务未完成，不以 q=60 或全回退替代。
- [x] 4.4 对上述完整候选执行 GDS 层级/多边形回读、真实金属连通、宽度/间距/via enclosure、光电窗口与外框检查，保存验证记录；重复生成最佳已验证候选，核对规范化几何 hash、设置、端口/pad 映射及所有尺寸路径统计。

## 5. C：8/16 端口排列实验

- [x] 5.1 为实验布局增加逻辑开关到物理行及 pin 的显式双射，保持规范 Network、逻辑 ID、求解器与外部编号不变；覆盖现有 normal/reverse 表达，并以遗漏、重复、错误 pin 和跨 stage pad 绑定证明独立映射检查有效。
- [x] 5.2 实现恒等、递归子组交换、子组反序与索引位反转的确定性候选搜索，固定首末级、去重和 tie-break，每规模最多 128 次逻辑评分；记录全网络逆序数/最大位移、预算和候选顺序，重复运行核对确定性。
- [x] 5.3 为 C 提供同一确定性相邻交换物理后端，将恒等及最多 8 个不同排列接入完整 IO、电学与四排 pad 布局；逐边从物理映射还原规范图并检查全局 crossing，确保主对照没有混用路由后端。
- [x] 5.4 分别完成 8、16 端口物理实验，保存恒等及非恒等候选的完整验证结果或具体拒绝证据，另列现版/A 参照并标明后端差异；结论区分代理改善、全局交叉变化和真实面积收益，未改善时明确已测预算，不推广到 100 端口。

## 6. 回归与交付

- [x] 6.1 运行相关逻辑路由、旧几何/折叠/行序/一至四排 pad、旧 bundle 兼容性及新增模块/故障注入回归，确认未改变 Waksman 和 Beneš 求解行为；汇总测试结果及全尺寸验证证据，未通过项不得勾选完成。
- [x] 6.2 更新使用说明、参数限制、布局预览和比较结论，明确占位器件、TFLN 允许方向、软 20 mm 目标是否达到及 C 实验边界；依据实际结果勾选本清单，核对变更范围并将实现、可再生配置和报告提交 Git。
