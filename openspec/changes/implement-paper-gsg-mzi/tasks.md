## 1. 器件与布线
- [x] 1.1 完成论文参数溯源及 1 mm 四端口实体 MZI，单元半径/连续性/电极测试通过并输出 GDS。
- [x] 1.2 集成公共地及独立 S，内部和外围 via 均提取，小规模矩阵 GDS 检查通过。
- [x] 1.3 扩展故障注入与旧模式兼容验证，完整测试通过。

## 2. 完整交付
- [x] 2.1 生成并验证 100/128 全矩阵，核对公共地、832 独立 S、器件长度和尺寸报告。
- [x] 2.2 重复生成、独立回读与旧版 bundle 回读通过，交付文档、图片和报告并提交 Git。

## 3. 延长主电极
- [x] 3.1 默认主电极延伸至 x=840 μm（有效长度 680 μm），两端留白对称，桥和引出跟随移动；验证半径、间距、共地及信号隔离。
- [x] 3.2 重新生成单元和完整矩阵，完成回归、重复生成及独立回读，更新图、文档和报告并提交 Git。

## 4. M2 连续引出
- [x] 4.1 显式定义 G/S 的 M2 端口，取消两处器件出口及两处外围入口 via；对齐及通道扇出均支持端口层，旧 M1 端口产物兼容回读。
- [x] 4.2 完成层接口故障注入及完整回归，重新生成、重复验证 100/128 矩阵，更新图、尺寸与 via 报告并提交 Git。
