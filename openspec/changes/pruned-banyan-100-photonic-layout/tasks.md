# Tasks

## 1. 配置与匹配裁剪图

- [x] 1.1 增加显式 `pruned-banyan` 配置、网络分派和独立100路示例；验证N≥2、默认最小2幂母网及匹配映射，拒绝非法规模/映射，并确认原配置序列化与哈希不变。
- [x] 1.2 实现设计中逐级缩小块的递归Banyan母网、全bar输出选择、公开0…N−1映射及稳定开关ID；以N=2、3、5、7、8、12、13、100、128核对全部单对唯一路径与满载参考映射。
- [x] 1.3 基于前后可达集合裁剪实际边/开关，导出未用器件端口；以独立路径并集核对100路400个MZI、逐列50/50/52/56/64/64/64、逐级100/100/104/112/128/128条边，以及28+28未用端口。
- [x] 1.4 加入原相对轨道顺序的交叉计数及端口选择反例；验证级间1225/600/312/168/96/32、总2433，并用独立最大流核对匹配选择可同时100路、两端前100口仅79路，防止使用错误裁剪集合。

## 2. 请求求解、阻塞与CLI

- [x] 2.1 实现仅对实际请求求唯一路径、合并bar/cross约束及链路占用检查，未约束开关默认bar；验证无冲突部分请求、空请求、请求顺序确定性和完整状态覆盖，以及重复/越界/非法请求拒绝。
- [x] 2.2 实现独立端口走图验证及明确阻塞诊断；验证冲突请求对和内部位置可定位，缺失状态、非法值、错接边、错误暗输入和活动路径进入终端均被拒绝，不把新模式报告为可重排无阻塞。
- [x] 2.3 接入generate/solve/verify与显式满载reference模式，区分省略连接和显式identity/reverse；验证新模式省略时使用真实满载参考、显式阻塞请求非零退出、旧模式仍默认identity。
- [x] 2.4 为新模式隔离求解/生成失败与发布流程；在预置成功包后提交阻塞请求，确认不会发布此次成功状态或把旧包当作此次结果，并核对错误/失败记录中请求身份和旧包处理行为。
- [x] 2.5 完成逻辑整体验收：N=2…8全部置换与独立状态枚举oracle比较，N=100全部10,000个单连接、全bar满载、种子17的100组满载子集和100个随机完整请求；核对成功/阻塞分类及每条有效路径7个MZI。

## 3. 稀疏连续光学与片内终端

- [x] 3.1 实现带有效轨迹掩码的连续shuffle层级与缓存键，按双轨/单轨/无轨保留crossing、替换直线或删除；用正反方向及不同掩码检查每条端点、切向、半径和净距，确认无删除支路残臂及错误缓存复用。
- [x] 3.2 将裁剪图接入稀疏物理列、实际边和母网行号到坐标映射，复用1 mm/680 µm GSG器件；检查不同列的器件与有效边清单对应，没有整块未裁剪母单元或幽灵波导进入导出层级。
- [x] 3.3 为未用MZI端口增加绑定到器件/pin的定向片内终端与合法引线；小规模验证每个物理端口恰有一个归属、终端朝向/连续性/禁布区正确，100路核对28输入端和28输出端，元数据保留未验证反射性能说明。
- [x] 3.4 实现保持顺序的外部100口适配，特别是稀疏输出到连续公开编号的适配；验证恰好100西入/100东出、无隐藏外部光口，全部适配曲线满足半径/净距并纳入长度及交叉统计。
- [x] 3.5 扩展独立光学验证到有效边、MZI全部端口、终端和crossing两路贯通；用漏/重复/错向终端、错接有效边、残留crossing支路、错误传输及过小半径故障证明检查能拒绝损坏结果。

## 4. 共享地、电学与有界布局候选

- [x] 4.1 接入左右电学变体及按实际光学/终端障碍分段的共享G轨，保证每轨一个接点与via；验证所有S/G探针连接，局部终端不会被共享金属跨越，接点数量由实际连续段得到。
- [x] 4.2 生成按源干线顺序排布的直接M2两排pad与公共地tap；100路验证400S+8G、南北各204个且每排102个、60 µm pad、同排≥100 µm、行距≥70 µm及外排引线净距，小规模验证数量随列数调整。
- [x] 4.3 接入34/35 µm、逐列左右出线和pad位置的确定性有限选择；验证每间距最多8个候选加全右出对照、全片候选总数≤18，按硬约束过滤后以实际面积/宽度/crossing/转折/线长择优。
- [x] 4.4 完成5、7、12、13路代表性全片生成与GDS回读，覆盖34/35 µm及左右出线；核对全部光学连接、终端、共享地、独立信号、pad、via和全片净距，形成100路生成前的物理验收结果。
- [x] 4.5 扩展真实金属提取与电学故障测试；用共享G开路、S-G桥接、丢失/重复via、浮空导体和pad间距破坏验证失败定位，确认未扩大器件或绝缘跨越豁免区域。

## 5. 指标、100路验收与交付

- [x] 5.1 扩展报告及预览，分列外部/母网/级间通道、图交叉/全片crossing、终端、MZI/pad/via/G接点、分区/整片包络和实际路径统计；检查2433只作为图级基线，适配与终端避让均计入物理总数。
- [x] 5.2 完成100路有限候选生成与选定布局的独立GDS验收；验证100输入/100输出、400个MZI、56个终端、408个pad、401个电学网络和全片crossing<4522，交付实际尺寸及全部淘汰原因；无合格候选时不得勾选本任务。
- [x] 5.3 两次重新生成选定100路配置、候选和请求，核对规范化几何/连接/状态/指标一致；运行项目全部相关及历史回归，验证原Beneš/AS-Beneš默认请求、哈希、无阻塞契约及参考生成/验证入口未改变。
- [x] 5.4 交付独立配置、固定候选选择、完整生成包与总览/端口压缩/片内终端/共享地/pad局部图；更新使用说明和相对现有AS-Beneš的对比，核对复现命令、路径链接、阻塞说明、20 mm软目标及未校准终端性能表述准确。
